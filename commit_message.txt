🧹 [Code Health] Remove hardcoded home directory fallback

🎯 What: Removed the hardcoded fallback paths of `"/home/julesklord"` in `src/agent.rs` and replaced them with standard, generic home directory resolution.
💡 Why: Improves maintainability and code health by making the fallback robust and independent of specific user accounts, eliminating a likely cause of cross-environment bugs.
✅ Verification: Ran `cargo test`, `cargo build`, and `cargo clippy -- -D warnings` successfully.
✨ Result: The codebase is cleaner, safely defaults to `"~"`, and utilizes `directories::BaseDirs::new()` where applicable.
